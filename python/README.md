# code-rookie-python

Working project for the [Python (+ PyTorch + Keras) study guide](00-overview.md) — start there. This directory is both the guide (the `.md` files) and a real, runnable `uv` project (`src/code_rookie_python/`, `tests/`) the guide's own capstones are built and verified against.
